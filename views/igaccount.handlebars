<!-- CSS Custom Styling for this Page -->
<head>
<link rel="stylesheet" type="text/css" href="css/account.css">
</head>

<!-- Nav Bar -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/igaccount" class="active">To D3 Or Not To D3<span class="sr-only">(current)</span></a>
      </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/c3visualization">Popularity (C3)</a></li>
        <li><a href="/visualization">D3</a></li>
        <li><a href="/d3visualization">New D3</a></li>
      </ul>
      <form class="navbar-form navbar-right" action="/logout" method="GET">
      	<input type="submit" class="btn btn-danger nav-btn" value="Logout">
      </form> 
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<!-- Content -->

	<div class="page-header center-block">
		<h1>Hello, @{{user.name}}!</h1>
		<h2>{{req.user.provider}}</h2> <!-- NOT WORKING -->
		<img class="img-responsive img-circle center-block" src="{{user_info.profile_picture}}">
	</div>

  <p><a href="/igselfphotos">TEST</a></p>
	<h3>Profile statics</h3>
	<p><strong>ID:</strong> {{user.id}}</p>
	<p><strong>Name: </strong>{{user.name}}</p>
	<p><strong>Instagram ID: </strong>{{user.ig_id}}</p>
	<h3>New Stats!</h3>
	<p><strong>Full Name: </strong>{{user_info.full_name}}</p>
	<h4>Number of:</h4>
	<p><strong>Uploaded Pics: </strong>{{user_info.counts.media}}</p>
	<p><strong>Followers: </strong>{{user_info.counts.followed_by}}</p>
	<p><strong>Follows: </strong>{{user_info.counts.follows}}</p>

